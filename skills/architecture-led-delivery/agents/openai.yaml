interface:
  display_name: "Architecture-led delivery"
  short_description: "Create and maintain source-linked architecture maps"
  default_prompt: "Use $architecture-led-delivery to create or update the relevant architecture map, grounding current-state claims in sources and separating proposals from approved decisions."
policy:
  allow_implicit_invocation: true

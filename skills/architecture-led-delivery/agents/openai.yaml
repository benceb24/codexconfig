interface:
  display_name: "Architecture-led delivery"
  short_description: "Direct delivery with bounded delegation"
  default_prompt: "Use $architecture-led-delivery to implement and verify this change against the maintained architecture, working directly unless bounded delegation materially helps."
policy:
  allow_implicit_invocation: false
